{
  "name": "actions-test",
  "version": "1.0.0",
  "license": "MIT",
  "main": "server.js",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "test": "NODE_ENV=test nyc --reporter=lcov mocha --exit",
    "watch": "nodemon -L -e js",
    "lint": "eclint check $(git ls-files) && xo"
  },
  "dependencies": {
    "bytes": "^3.1.0",
    "config": "^3.3.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eclint": "^2.8.1",
    "http-string-parser": "^0.0.6",
    "mocha": "^8.4.0",
    "nodemon": "^2.0.4",
    "nyc": "^15.1.0",
    "supertest": "^4.0.2",
    "xo": "^0.33.0"
  }
}
